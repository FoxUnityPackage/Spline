using UnityEngine;

[RequireComponent(typeof(MeshFilter))]
public class RuntimePathCreator : MonoBehaviour
{
    [SerializeField] public Spline spline;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
